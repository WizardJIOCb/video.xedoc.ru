server {
    listen 80;
    listen [::]:80;
    server_name video.xedoc.ru;

    root /var/www/video.xedoc.ru/current;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
